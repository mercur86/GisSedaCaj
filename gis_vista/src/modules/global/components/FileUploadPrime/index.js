import React from 'react';
import {FileUpload} from 'primereact/fileupload';

const FileUploadPrime = ({...otrasPropiedades}) => (
    <FileUpload {...otrasPropiedades}/>
);

export default FileUploadPrime;